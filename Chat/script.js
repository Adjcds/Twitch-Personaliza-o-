// Por favor, use event listeners para executar funções.
document.addEventListener('onLoad', function(obj) {
    // obj estará vazio para o widget de chat
    // isso será disparado apenas uma vez quando o widget carregar
});

document.addEventListener('onEventReceived', function(obj) {
    // obj conterá informações sobre o evento
});
